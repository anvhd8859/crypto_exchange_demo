import React from 'react';
import {View, Text} from 'react-native';
import styles from './styles';

const MarketScreen = (props) => {
    return (
        <View style={styles.container}>
            <Text>I am MarketScreen</Text>
        </View>
    );
};

export default MarketScreen;
