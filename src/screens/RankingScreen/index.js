import React from 'react';
import {View, Text} from 'react-native';
import styles from './styles';

const RankingScreen = (props) => {
    return (
        <View style={styles.container}>
            <Text>I am RankingScreen</Text>
        </View>
    );
};

export default RankingScreen;
